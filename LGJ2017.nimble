# Package/application
version = "1.0"
author "Benjamin N. Summerton"
description = "My submission for the Linux Game Jam 2017"

# Deps
requires "nim >= 0.16.0"
requires "strfmt >= 0.8.4"
requires "stopwatch >= 3.3"
requires "opengl >= 1.1.0"
requires "sdl2_nim >= 0.96"
